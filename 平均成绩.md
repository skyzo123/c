```
#include <iostream>
#include <math.h>
#include <stdio.h>
#include <iomanip>
using namespace std;
int main() {
    long a;
    long b;
    double q,w,e,r;
    int i;
    while (scanf_s("%d%d", &a, &b) != EOF)
    {
        for (int j = 0; j < a; ++j)
        {
            for (int k = 0; k < b; ++k)
            {
                cin>>q;
                w+=q;
            }
        }
    }
}
```

