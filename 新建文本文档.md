```
#include <iostream>
#include <math.h>
#include <stdio.h>
#include <iomanip>
using namespace std;
int main()
{
    int a,q,e,r,c,mun=0;
    int b=0,w;int i;
    double x,y;
    while(1) {
        cin >> a >> e;
        if (a != 0 || b != 0)
        {
            for (i = a; i <= b; i++)
            {
                mun=0;
            }

        if (mun == 0)
            cout << "OK" << "\n";
        else
            cout << "Sorry" << "\n";
        }
        else
        break;
    }
    return 0;
}
```